# Python packages for venv
flask
flask-socketio
eventlet
pandas
pyyaml
sounddevice
soundfile
watchdog
scipy
numpy

# System packages (install via apt/pacman, not pip):
# - python3-hamlib (Debian) / python-hamlib (Arch)
# - libsndfile1 (for MP3 support in soundfile)
